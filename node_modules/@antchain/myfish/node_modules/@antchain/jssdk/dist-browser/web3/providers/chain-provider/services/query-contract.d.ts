import { TLSConnector } from '../tls-connector';
import { Hash } from "../../../../types/web3";
export interface QueryContractPrarams {
    contractName: string;
}
export interface QueryContractResult {
    msg_type: number;
    sequence: number;
    return_code: number;
    group_id: string;
    data: {
        identity: string;
        balance: number;
        recover_key: string;
        recover_time: number;
        status: number;
        code: '';
        storage_root: Hash;
        code_hash: Hash;
        encryption_key: '';
        acc_version: number;
        auth_map: {
            auth_key: string;
            auth_weight: number;
        }[];
    };
    api: 'QueryContract';
}
export declare function queryContract(params: QueryContractPrarams, connector: TLSConnector): Promise<QueryContractResult>;
