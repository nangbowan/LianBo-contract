import { TLSConnector } from '../tls-connector';
import { EventListenerType } from "../../../../types/web3";
export interface AddListenerParams {
    contractName: string;
    type: EventListenerType;
}
export interface AddListenerResult {
    filter_id: string;
}
export interface PullEventsParams {
    filterId: string;
}
export interface PullEventsResult {
    msg_type: number;
    sequence: number;
    return_code: number;
    group_id: string;
    event_type: EventListenerType;
    event_data: any;
    api: 'EventFetch';
}
export declare function addListener(params: AddListenerParams, connector: TLSConnector): Promise<AddListenerResult>;
export declare function pullEvents(params: PullEventsParams, connector: TLSConnector): Promise<PullEventsResult>;
