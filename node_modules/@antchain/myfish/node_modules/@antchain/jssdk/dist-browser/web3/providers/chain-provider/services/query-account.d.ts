import { TLSConnector } from '../tls-connector';
import { Account } from "../../../../types/web3";
export interface QueryAccountPrarams {
    account: Account;
    groupId?: string;
}
export interface QueryAccountResult {
    msg_type: number;
    sequence: number;
    return_code: number;
    group_id: string;
    data: {
        identity: string;
        balance: number;
        recover_key: string;
        recover_time: number;
        status: number;
        code: string;
        storage_root: string;
        code_hash: string;
        encryption_key: string;
        acc_version: number;
        auth_map: any[];
    };
    api: 'QueryAccount';
}
export declare function queryAccount(params: QueryAccountPrarams, connector: TLSConnector): Promise<QueryAccountResult>;
