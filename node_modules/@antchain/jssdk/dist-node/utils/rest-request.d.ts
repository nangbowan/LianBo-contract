import { AccessId, AccessSecret } from "../types/web3";
export interface RequestResponse {
    success: boolean;
    code: string;
    data: any;
    message?: string;
}
export interface RestRequestConfig {
    baseUrl: string;
    accessId: AccessId;
    accessSecret: AccessSecret;
}
export declare class RestRequest {
    private requestConfig;
    tokenPromise: Promise<string>;
    constructor(config: RestRequestConfig);
    getToken(): Promise<string>;
    request(url: string, body?: any, tokenTimeoutRetryTimes?: number): Promise<any>;
}
