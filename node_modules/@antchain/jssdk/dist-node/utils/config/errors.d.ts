import { JssdkErrorInfo, ErrorType } from "../../types/error";
export declare class SDKError extends Error {
    type: ErrorType;
    code: string;
    returnCode: string;
    receiptCode: string;
    hash: string;
    output: string;
    constructor(msg: string, extInfo?: JssdkErrorInfo);
}
declare const errors: {
    InvalidNumberOfSolidityArgs(): SDKError;
    connection(host: string): SDKError;
    response(): SDKError;
    timeout(): SDKError;
    unauthorized(): SDKError;
    api(name: string): SDKError;
    param(name: string): SDKError;
    contractDeploy(name: string): SDKError;
    returnCode(code: string): SDKError;
    receiptResult(result: string, hash: string, output: string): SDKError;
    codeResult(code: string, result: string): SDKError;
};
export default errors;
