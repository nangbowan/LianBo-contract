// Variant 是动态数据类型的实现，详见 https://github.com/MaxGraey/as-variant
import { Variant } from './deps/as-variant';

// 给 Variant.from 起一个别名，透出给用户
export function Var<T>(value: T): Variant {
  return Variant.from(value);
}
