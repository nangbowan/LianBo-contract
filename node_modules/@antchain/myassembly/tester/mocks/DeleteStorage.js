module.exports =
  ({ __storage, __getString }) =>
  (key_ptr, key_length) => {
    const key = __getString(key_ptr, key_length);
    if (__storage.has(key)) {
      __storage.delete(key);
    }
    return 0;
  };
