module.exports =
  (memory) =>
  (str_ptr, str_length, encoding = 'utf8') => {
    const buf = Buffer.from(memory.buffer.slice(str_ptr, str_ptr + str_length));
    return buf.toString(encoding);
  };
